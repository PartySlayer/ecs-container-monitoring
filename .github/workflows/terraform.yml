name: "Terraform CI - Plan & Simulation"

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  terraform-plan:
    name: 'Terraform Plan'
    runs-on: ubuntu-latest
    
    environment: development

    defaults:
      run:
        shell: bash
        working-directory: ./infra

    steps:
    # 1. Checkout del codice
    - name: Checkout
      uses: actions/checkout@v3

    # 2. Configura le credenziali AWS (precedentemente salvate nei Secrets di GitHub)
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: arn:aws:iam::648758970980:role/terraform-role-container-monitoring  # output bootstrap
        aws-region: eu-west-1

    # 3. Installa Terraform
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.14.3

    # 4. Lint o quasi
    - name: Terraform Format
      run: terraform fmt -check

    # 5. Inizializzazione
    - name: Terraform Init
      run: terraform init

    # 6. Altro lint o quasi
    - name: Terraform Validate
      run: terraform validate

    # 7. Plan poi apply
    - name: Terraform Plan
      run: terraform plan -input=false

    # 8. Apply, creazione delle risorse solo quando pushato su main
    - name: Terraform Apply
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false